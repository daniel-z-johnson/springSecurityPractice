<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.practice.security.mapper.ProfileMapper">
    
    <resultMap id="ProfileResultMap" type="com.practice.security.model.Profile">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="favoriteTechnologies" column="favorite_technologies" 
                typeHandler="org.apache.ibatis.type.ArrayTypeHandler"/>
        <result property="favoriteVideoGames" column="favorite_video_games" 
                typeHandler="org.apache.ibatis.type.ArrayTypeHandler"/>
        <result property="favoriteBooks" column="favorite_books" 
                typeHandler="org.apache.ibatis.type.ArrayTypeHandler"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <insert id="insertProfile" parameterType="com.practice.security.model.Profile" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO profiles (user_id, favorite_technologies, favorite_video_games, favorite_books, created_at, updated_at)
        VALUES (#{userId}, 
                #{favoriteTechnologies, jdbcType=ARRAY, typeHandler=org.apache.ibatis.type.ArrayTypeHandler}::text[], 
                #{favoriteVideoGames, jdbcType=ARRAY, typeHandler=org.apache.ibatis.type.ArrayTypeHandler}::text[], 
                #{favoriteBooks, jdbcType=ARRAY, typeHandler=org.apache.ibatis.type.ArrayTypeHandler}::text[], 
                CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>
    
    <select id="findByUserId" resultMap="ProfileResultMap">
        SELECT id, user_id, favorite_technologies, favorite_video_games, favorite_books, created_at, updated_at
        FROM profiles
        WHERE user_id = #{userId}
    </select>
    
    <update id="updateProfile" parameterType="com.practice.security.model.Profile">
        UPDATE profiles
        SET favorite_technologies = #{favoriteTechnologies, jdbcType=ARRAY, typeHandler=org.apache.ibatis.type.ArrayTypeHandler}::text[],
            favorite_video_games = #{favoriteVideoGames, jdbcType=ARRAY, typeHandler=org.apache.ibatis.type.ArrayTypeHandler}::text[],
            favorite_books = #{favoriteBooks, jdbcType=ARRAY, typeHandler=org.apache.ibatis.type.ArrayTypeHandler}::text[],
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
    
</mapper>
